# This file is for reference only - actual secrets are set via SST
# Use: sst secret set <NAME> <VALUE>

# JWT Secrets (set these via CLI)
# sst secret set JWT_SECRET your-production-jwt-secret-min-32-chars
# sst secret set JWT_REFRESH_SECRET your-production-refresh-secret
# sst secret set SOLANA_SERVER_SECRET your-base64-server-wallet-secret

# Database will be auto-configured by SST
# Solana config in sst.config.ts
